import type { NextConfig } from "next";
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'i.imgur.com', 'placehold.co', 'via.placeholder.com', 'placeimg.com'],
  },
}

module.exports = nextConfig;

